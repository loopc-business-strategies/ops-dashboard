#!/usr/bin/env node
/**
 * Placeholder only. Venus/MG operational cleanup is NOT enabled in this wave.
 * Run npm run audit:tenant-isolation first and wait for explicit approval.
 */
console.error('cleanup:vb-mg-data is disabled.')
console.error('1. Run: npm run audit:tenant-isolation')
console.error('2. Review reports/VENUS-DATA-CONTAMINATION-REPORT.md')
console.error('3. Approve a gated quarantine/delete plan before any write script is added.')
process.exit(1)
