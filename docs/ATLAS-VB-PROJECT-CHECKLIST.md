# Atlas UI checklist — Venus Bullions dedicated project

## Complete

- Project **Venus Bullion** + Cluster0 (`cluster0.fiotefu.mongodb.net`)
- DB user `business_db_user`
- Network Access: `0.0.0.0/0` (Railway) + office IP
- Data migrated from MG `ops-dashboard-vb` → VB `ops-dashboard` (+ staging DB)
- Railway prod/staging `MONGO_URI_VB` pointed at dedicated host
- Old MG `ops-dashboard-vb` dropped after verify
