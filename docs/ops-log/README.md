# Ops log (local)

Gitignored drill and maintenance records. Use:

```bash
npm run verify:backup-checklist -- --record "restored MG snapshot to non-prod cluster"
```

Do not commit real cluster names or credentials in log entries.
